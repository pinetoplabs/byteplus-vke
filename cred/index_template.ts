import type { BytePlusCredentialsConfig } from '../src/types'

export const BP_CONFIG = {
    credentials: {
        accessKeyId: '',
        secretAccessKey: ''
    } as BytePlusCredentialsConfig,
    region: ''
}
